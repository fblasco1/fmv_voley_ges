[project]
name = "fmv-voley-ges"
version = "0.1.0"
description = "Extractor de fixtures FMV (metrovoley.com.ar) para ICDPE / SICLUB"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "requests>=2.31.0,<3",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
